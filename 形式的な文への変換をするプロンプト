system:
あなたには今から、自然言語の機能要求文を形式的な文へと変換してもらいます。
    具体的には、機能要求文を[名詞]+[格助詞]+[名詞]+[格助詞]+...+[動詞（助動詞を含む）]という形式に変換してもらいます。
    [名詞]の部分について、名詞句や補足節、連体節等の「修飾部分を伴う名詞」でも良いです。
    [格助詞]の部分について、「によって」や「について」等の複合格助詞を用いても良いです。しかし、「は」や「も」等の副助詞や、副助詞を含む複合助詞を用いてはいけません。
    また、[動詞]の部分は、指定された動詞にしてください。ただし、能動態でも受動態でも構いません
    （例えば、動詞として「入力する」が指定された場合、[動詞]の部分には「入力する」か「入力される」のいずれかを当てはめてください）。
    例えば、「解析システムがデータを出力する」という文は「解析システム」と「データ」が名詞であり「が」と「を」が格助詞であり「出力する」が動詞であるため、先述した形式として妥当な文です。
    この変換時には、文の変換前と後とで意味が大きく変化しないようにしてください。
    解析の出力は、JSON型式で出力し、{"出力":"変換後の形式的な文"}という形で出力してください。
    以下、処理の例を示します。
    例１．入力：「利用者がデータ検索を行う。」、指定された動詞「検索する」　この文について、データを名詞とみなし、「検索する」を動詞とするように変換すると考えると出力は次のようになる。　出力：{"出力":"利用者がデータを検索する"}
    例２．入力：「そのデータは指定されたデータベースに挿入する」、指定された動詞「挿入する」　この文について、「は」は副助詞であるため意味が近い格助詞に置き換える。また、「指定されたデータベース」は修飾部分を伴った名詞と見なせる。よって出力は次のようになる。　出力：{"出力":"そのデータを指定されたデータベースに挿入する"}
    例３．入力：「表示されたデータ」、指定された動詞「表示する」　この文について、「データ」は表示された内容である。また、表示されたは受動態である。これらを踏まえると出力は次のようになる。　出力：{"出力":"データが表示される"}
    例４．入力：「システムは『データA』『データB』を表示する」、指定された動詞「表示する」　この文について、「は」は副助詞であるため、意味が近い格助詞の「が」に置き換える。また、『データA』と『データB』は並列された名詞であるため、格助詞「と」を挿入する。　出力：{"出力":"システムが『データA』と『データB』を表示する"}
    例５．入力：「出力機能がデータの出力をする」、指定された動詞「出力する」　この文について、「データ」の後続に格助詞「の」が続いているが、「出力する」を動詞とするような文に変換するにあたり、格助詞「の」を「を」にすることで意味が近い文になる。　出力：{"出力":"出力機能がデータを出力する"}


user:
「<文節>」という文を、形式的な文へと変換し、{"出力":"変換後の形式的な文"}という形で出力してください。その文は「<原文>」が分解処理された文ですが、欠けている要素があれば分解前の文から抜き出し、所定の形式の文へと変換してください。この文の動詞は「<動詞>」です。


注意点:
<文節>には，4.2節で出力された節が入る．
<原文>には，解析対象文の原文が入る．
<動詞>には，4.2節で特定された動詞が入る．
